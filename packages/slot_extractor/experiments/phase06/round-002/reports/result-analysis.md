# Round 002：结果分析

## 实验与完整性

- 训练数据：`sft-v0.3`，两个模型各 976 条训练、109 条验证；
- 评估：冻结的 `eval-v0.2` 共 51 条，修正版评分器离线统一复算；
- 云端结果包共 93 个文件，SHA-256 全部通过；
- Adapter 仅在本地外部归档，Git 只保存哈希、训练指标和逐样本结果。

两个 run 均完成 3 epoch、183 step，验证 loss 连续下降，没有发散或验证集反弹：

| 模型 | train loss | eval loss | 训练耗时 |
|---|---:|---:|---:|
| Qwen3-0.6B | 0.0942 | 0.0378 | 9 分 46 秒 |
| Qwen3-1.7B | 0.1175 | 0.0463 | 16 分 19 秒 |

## 总体结果

| 模型 | Round 001 | Round 002 | 净变化 | 协议 | 任务均分 | 本轮目标 |
|---|---:|---:|---:|---:|---:|---|
| Qwen3-0.6B | 33/51 | 36/51 | +3 | 50/51 | 95.95% | 37/51，未达成 |
| Qwen3-1.7B | 34/51 | 34/51 | 0 | 49/51 | 91.39% | 38/51，未达成 |

0.6B 新通过 7 条、退化 4 条；1.7B 新通过 4 条、退化 4 条。两个模型的回归数都超过“不多于 1 条”的护栏，因此即使 0.6B 均分和协议显著改善，本轮也不能判为成功。

## 0.6B 分析

有效收益集中在重选、重试、方案修改和工程师继承：`specific-unavailable-01-reselect`、`specific-not-found-01-reselect`、`search-no-match-01-retry`、`plan-modify-01-call`、`multiturn-tech-01-call/result` 转为通过。不可解析输出由上一轮 4 条降至 1 条，证明“严格短 JSON + tool_call 专项”有效。

残余错误仍集中在：

- 将“售后服务”等通用服务词写入 `preferences`；
- 多轮相对日期算错或继承旧日期；
- 工具结果转 final 时漏掉 `engineer_level`；
- unavailable/not_found 后选择了错误的回复动作；
- 简单缺失信息追问发生表达退化。

## 1.7B 分析

`matched → available` 已修复为 0 条非法枚举，部分工具结果映射和缺失信息样本跨过门槛。但新通过与退化各 4 条，没有净收益，任务均分还从 Round 001 的 93.59% 降至 91.39%。

最严重的新回归是 `specific-unavailable-01-reselect-result` 和 `plan-modify-01-result` 直接输出自然语言而非 JSON；另有无关请求识别、工程师能力等级保留和多轮日期继承错误。这说明专项样本修复了枚举，却没有形成足够强的“所有 final 必须为完整 JSON”约束和旧能力回放。

## 归因与决定

训练过程健康，问题主要来自关键交叉条件覆盖和回归保护不足，不支持通过继续加 epoch 解决。当前错误仍是有唯一正确标签的结构化状态错误，不是偏好排序问题，因此下一轮继续 SFT，不进入 DPO。

状态：尚未执行实验。
