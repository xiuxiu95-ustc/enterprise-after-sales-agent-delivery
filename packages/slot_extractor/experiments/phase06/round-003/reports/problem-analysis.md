# Round 003：问题分析

Round 002 的训练过程健康，但两个模型均有 4 条旧通过样本退化。0.6B 的专项工具调用训练有效，严格通过由 33/51 提升到 36/51；1.7B 修复了非法 `matched`，却出现两条纯文本 final，最终仍为 34/51。

当前失败不是“回答风格偏好”问题，而是存在唯一正确标签的结构化错误：字段归属、相对日期、状态继承、工具结果映射和 JSON 协议。因此 Round 003 继续 SFT，DPO 延后。

具体问题、证据和假设见 [problems.yaml](../problems.yaml)。
