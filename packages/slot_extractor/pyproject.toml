[project]
name = "enterprise-slot-extractor"
version = "0.2.0"
description = "Fine-tuning, evaluation, quantization and inference toolkit for enterprise service appointment slots."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "peft==0.18.1",
    "pyyaml>=6.0.1",
    "sentencepiece>=0.2.2",
    "sentence-transformers>=5.0.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
slot-eval = "scripts.eval.run_eval:main"
slot-build-dataset = "scripts.data.build_dataset:main"

[dependency-groups]
dev = [
    "pytest>=8.2.0",
    "ruff>=0.5.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = [".claude", ".superpowers"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# Test fixtures embed exact-schema JSON as single-line string literals;
# keeping them verbatim is worth more than the 100-char line limit.
"tests/**/*.py" = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
addopts = "-m 'not local_backend'"
markers = [
    "local_backend: requires a running local llama-server",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/slot_extractor", "scripts"]

[tool.hatch.build]
dev-mode-dirs = ["src", "."]
